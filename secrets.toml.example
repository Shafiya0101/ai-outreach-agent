# Local Streamlit secrets. Copy to .streamlit/secrets.toml and fill in.
# NEVER commit the real secrets.toml (it's in .gitignore).
MISTRAL_API_KEY = "your-new-mistral-key"
TAVILY_API_KEY = "your-tavily-key-optional"
APP_PASSWORD = "pick-a-demo-password"
